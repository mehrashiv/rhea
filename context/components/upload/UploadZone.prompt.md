Dashed photo dropzone for "Share a photo". Click opens the file picker; drag-over tints it lilac. Visual + basic file pickup only.

```jsx
<UploadZone onFile={(file) => setDraft(file)} />
```

Title is Cormorant; the whole well is the click target.
